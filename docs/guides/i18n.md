# Internationalization (i18n)

Queopius Shield dashboard supports language switching through the host app locale flow.

Current locale set:

- ES
- EN
- FR
- PT-BR (`pt` locale key)

## How it works

- Locale switch endpoint: `/locale/{locale}`
- Session stores selected locale
- Shield UI reads translated labels from app lang files (`lang/<locale>/shield.php`)

## Required app setup

- Include locales in `config/app.php`:

```php
'supported_locales' => ['es', 'en', 'fr', 'pt'],
```

- Ensure locale middleware runs on web requests.

## Translation files

Add/edit dashboard labels in:

- `lang/en/shield.php`
- `lang/es/shield.php`
- `lang/fr/shield.php`
- `lang/pt/shield.php`

## Add a new locale

1. Add locale code to `supported_locales`.
2. Create `lang/<new-locale>/shield.php`.
3. Add language option label in Shield dashboard locale selector if needed.
4. Clear cache:

```bash
php artisan optimize:clear
```
